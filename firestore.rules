rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow access to company subcollections
    // For development: allows all access
    // For production: add authentication check: if request.auth != null
    match /company/{companyId}/{document=**} {
      allow read, write: if true;
    }
  }
}

